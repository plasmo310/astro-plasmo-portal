---
import { Icon } from "astro-icon/components";
import SectionHeading from "../common/section-heading.astro";
import data from "../../data/skills.json";

interface SkillItem {
  name: string;
  level: number;
  icon: string;
}

interface SkillCategory {
  category: string;
  skills: SkillItem[];
}

interface SkillSection {
  section: string;
  categories: SkillCategory[];
}

const skillSections: SkillSection[] = data.skill as unknown as SkillSection[];
---

<section
  id="skills"
  class="flex items-start justify-center text-center px-6 py-16 bg-background-sub/70/25"
>
  <div class="container mx-auto">
    <SectionHeading title="Skills" />

    <div class="space-y-12 text-left max-w-6xl mx-auto">
      {
        skillSections.map((section) => (
          <div>
            <h3 class="text-lg font-heading font-semibold text-accent mb-8 tracking-widest uppercase border-b border-white/10 pb-3">
              {section.section}
            </h3>
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
              {section.categories.map((cat) => (
                <div class="skill-card bg-background-sub/70 border border-white/10 rounded-2xl p-6 hover:border-accent/20 transition-all duration-300">
                  <h4 class="text-gray-400 text-[11px] uppercase tracking-[0.4em] mb-4">
                    {cat.category}
                  </h4>
                  <div class="flex flex-col gap-3">
                    {cat.skills.map((skill) => (
                      <div class="group flex items-center gap-3">
                        <Icon
                          name={skill.icon}
                          class="w-7 h-7 shrink-0 group-hover:scale-110 transition-transform duration-300"
                        />
                        <span class="font-body text-white/90 text-sm font-medium flex-1">
                          {skill.name}
                        </span>
                        <div class="flex gap-0.5 shrink-0">
                          {[1, 2, 3].map((star) => (
                            <Icon
                              name="mdi:star"
                              class={`w-3 h-3 ${star <= skill.level ? "text-accent" : "text-gray-600"}`}
                            />
                          ))}
                        </div>
                      </div>
                    ))}
                  </div>
                </div>
              ))}
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>

<style>
  .scroll-stat {
    opacity: 0;
    transform: translateY(20px) scale(0.95);
    transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .scroll-stat.reveal {
    opacity: 1;
    transform: translateY(0) scale(1);
  }

  .skill-card {
    opacity: 0;
    transform: translateY(20px) scale(0.95);
    transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .skill-card.reveal {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
</style>

<script>
  function revealCards() {
    const scrollStats = document.querySelectorAll("#skills .scroll-stat");
    const observer1 = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const index = Array.from(scrollStats).indexOf(
              entry.target as HTMLElement,
            );
            setTimeout(
              () => {
                entry.target.classList.add("reveal");
              },
              (index % 3) * 150,
            );
            observer1.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.15, rootMargin: "0px 0px -10px 0px" },
    );
    scrollStats.forEach((card) => observer1.observe(card));

    const skillCards = document.querySelectorAll(".skill-card");
    const observer2 = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const index = Array.from(skillCards).indexOf(
              entry.target as HTMLElement,
            );
            setTimeout(
              () => {
                entry.target.classList.add("reveal");
              },
              (index % 6) * 50,
            );
            observer2.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.1, rootMargin: "0px 0px -50px 0px" },
    );
    skillCards.forEach((card) => observer2.observe(card));
  }

  revealCards();
  document.addEventListener("astro:after-swap", revealCards);
</script>
