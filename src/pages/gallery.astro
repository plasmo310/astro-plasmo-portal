---
import GalleryPage from "../components/pages/gallery-page.astro";
import { DataLoader } from "../lib/data-loader";

const loader = new DataLoader("ja");
---

<GalleryPage locale="ja" data={loader.gallery()} />
